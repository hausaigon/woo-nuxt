// import Vue from 'vue'

export default ({ app }) => {
  app.router.afterEach(() => {
    //to, from parameter
    // const appLoading = document.getElementById('appLoading')
    // appLoading.classList.remove('is-loading')
  })
}

// import { gsap } from 'gsap'
/**
 * @class itemParallax
 */
export class interactiveScroller {
  // constructor() {
  //   this.scroller = document.querySelector('.interactive-scroller')
  //   this.scroller__cursor = this.scroller.querySelector(
  //     '.interactive-scroller__cursor'
  //   )
  //   const screenTP = s.offset.y,
  //     screenH = 0,
  //     calc = (screenTP / (screenH - window.innerHeight)) * 100
  //   if (calc > 0) {
  //     this.scroller.classList.remove('is-first')
  //     gsap.set(this.scroller__cursor, {
  //       top: calc + '%',
  //     })
  //   } else {
  //     this.scroller.classList.add('is-first')
  //   }
  // }
  // init() {
  //   Scroll.addListener((s) => {
  //      const screenTP = s.offset.y
  //   })
  // }
}
